# Expensive packaging/deployment smoke gates.
# Format: name|path|command
# All three fields are required. Put commands containing a literal | in a project script and invoke that script here.
# Example:
# docker|.|bash scripts/docker-smoke.sh
