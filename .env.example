# Safe placeholders only. Never put real values in this file.
# Copy to .env.local and fill in there; .env.local is git-ignored.
#
# Add one line per variable the project actually needs.

# DATABASE_URL=
# API_BASE_URL=
